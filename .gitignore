/bin/
/dist/
/smolanalytics
*.out
.DS_Store

# runtime data — the event log and all its sidecar files (.insights.json, .settings.json,
# .audit.jsonl, .cohorts.json, .webhooks.json, .alerts.json, .compact), plus local artifacts
*.data
*.data.*
smolanalytics.data*
.playwright-mcp/
.env
.env.*

# internal strategy — never publish
SPEC.md
gtm/
*-STRATEGY.md
NOTES.md
